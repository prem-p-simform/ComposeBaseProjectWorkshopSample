package com.simform.design.theme.colors

import androidx.compose.ui.graphics.Color

object AppColorTokens {
    val primary = AppColorToken(
        light = Color(0xFF6974DB),
        dark = Color(0xFF6D58F4)
    )
    val secondary = AppColorToken(
        light = Color(0x666974DB),
        dark = Color(0x666D58F4)
    )
    val textColor = AppColorToken(
        light = Color(0xFF2C2B32),
        dark = Color(0xFFD5C7F4)
    )
    val bgColor = AppColorToken(
        light = Color(0xFFFFFFFF),
        dark = Color(0xFF000000)
    )
    val white = AppColorToken(
        light = Color(0xFFFFFFFF),
        dark = Color(0xFFFFFFFF)
    )
    val textFieldContainerColor = AppColorToken(
        light = Color(0xFFF1F1FD),
        dark = Color(0xFF1E1844)
    )
    val textFieldBorderColor = AppColorToken(
        light = Color(0xFF6974DB),
        dark = Color(0xFF6D58F4)
    )
    val error = AppColorToken(
        light = Color(0xFFD03B3B),
        dark = Color(0xFFD03B3B)
    )
    val success = AppColorToken(
        light = Color(0xFF10C054),
        dark = Color(0xFF10C054)
    )
    val switchThumbColor = AppColorToken(
        light = Color(0xFFE8EAEA),
        dark = Color(0xFFE8EAEA)
    )
    val checkedSwitchBorderColor = AppColorToken(
        light = Color(0xFF76EE59),
        dark = Color(0xFF76EE59)
    )
    val uncheckedSwitchBorderColor = AppColorToken(
        light = Color(0xFFEFEFEF),
        dark = Color(0xFF54536A)
    )
    val checkedSwitchBackgroundColor = AppColorToken(
        light = Color(0xFF76EE59),
        dark = Color(0xFF76EE59)
    )
    val uncheckedSwitchBackgroundColor = AppColorToken(
        light = Color(0xFFEFEFEF),
        dark = Color(0xFF54536A)
    )
    val tabContainerColor = AppColorToken(
        light = Color(0xFFF1F1FD),
        dark = Color(0xFF1E1844)
    )
    val ripple = AppColorToken(
        light = Color(0xFF000000),
        dark = Color(0xFFFFFFFF),
    )
    val bottomSheetScrim = AppColorToken(
        light = Color(0xCC000000),
        dark = Color(0xCCFFFFFF),
    )
    val statusBarColor = AppColorToken(
        light = Color(0xFFFFFFFF),
        dark = Color(0xFF000000),
    )
}